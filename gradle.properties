kotlin.code.style=official

# 构建提速：三个模块的编译 / 测试任务彼此独立，缓存也可以跨机器复用。
# （configuration cache 暂不打开：shadow / foojay 解析器对它的支持还在演进，
#   打开前先在 CI 上跑一轮 ./gradlew build --configuration-cache 验证。）
org.gradle.caching=true
org.gradle.parallel=true
